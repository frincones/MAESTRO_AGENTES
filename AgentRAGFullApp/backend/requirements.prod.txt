# Production requirements - CPU only, no CUDA
# Installs torch CPU-only to avoid 2GB+ NVIDIA downloads

# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pyyaml>=6.0

# LLM & Embeddings
openai>=1.12.0

# Database
asyncpg>=0.29.0

# Re-ranking (CPU only)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers>=2.2.0

# Document Processing
docling>=2.0.0
transformers>=4.36.0

# Lightweight readers
pypdf>=6.0.0
python-docx>=1.1.0

# Legal Sources
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Utilities
python-multipart>=0.0.6
python-dotenv>=1.0.0
